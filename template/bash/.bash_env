#!/usr/bin/env bash
#
# .bash_env : DO NOT EDIT -- this file has been generated automatically
#
# Ref.: dotfiles/template/bash/.bash_env

export EDITOR="vi"

if [ -d "${HOME}/.pyenv" ]; then
    export PATH="${HOME}/.pyenv/bin:$PATH"

    export PYENV_VIRTUALENV_DISABLE_PROMPT=1
    eval "$(pyenv init -)"
    eval "$(pyenv virtualenv-init -)"
fi
